// register.ts — ponto de entrada do APM Agent.
// Carregado via: node -r /opt/apm/dist/register.js app.js
// Sem alterar nenhuma linha de código da aplicação cliente.

import './sdk';
import './profiler';
